name: OIS Governance

on:
  pull_request:
    branches: [main]

# Explicitly declare safe default permissions
permissions:
  contents: read

jobs:
  governance:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v7

    - name: Set up Python 3.12
      uses: actions/setup-python@v7
      with:
        python-version: "3.12"
        cache: 'pip' # Automatically caches packages found in requirements.txt

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then python -m pip install -r requirements.txt; fi
        python -m pip install pytest # Ensures pytest is present if not in requirements

    - name: Run governance gate
      run: python scripts/ois_governance_check.py

    - name: Run test suite
      run: python -m pytest -q
