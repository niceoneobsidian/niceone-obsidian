name: OIS web branch autoformat

on:
  push:
    branches:
      - fix/web-intelligence-v1

permissions:
  contents: write

jobs:
  format:
    if: github.ref == 'refs/heads/fix/web-intelligence-v1'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Format and lint-fix
        run: |
          python -m pip install --upgrade pip
          python -m pip install "ruff==0.9.10"
          ruff check . --fix
          ruff format .
      - name: Commit formatting
        run: |
          rm .github/workflows/ois-web-autoformat.yml
          git config user.name "Niceone Obsidian"
          git config user.email "niceoneobsidian@gmail.com"
          git add -A
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "style: format web intelligence implementation [skip ci]"
          git push
