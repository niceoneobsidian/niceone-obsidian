# Canonical OIS Python tooling configuration; TOML regexes use literal-string syntax.
[project]
name = "niceone-obsidian"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2.7,<3",
  "psycopg[binary]>=3.3.4,<4",
]

[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = [
  "ois*",
  "ops*",
  "production*",
]

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [
  ".git",
  ".venv",
  "venv",
  "build",
  "dist",
  ".mypy_cache",
  ".pytest_cache",
  ".ruff_cache",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "B",
  "UP",
  "SIM",
]

# The Social Intelligence inventory is a declarative, machine-maintained manifest.
# Its long structure lists are intentionally kept readable as grouped data rather
# than reformatted into generated noise. Keep normal linting and type checking for
# the module, while allowing the manifest's long data rows and formatting to remain stable.
[tool.ruff.lint.per-file-ignores]
"ois/domains/social_intelligence/inventory.py" = ["E501"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"
exclude = [
  "ois/domains/social_intelligence/inventory.py",
]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
disallow_untyped_defs = true
check_untyped_defs = true
no_implicit_optional = true
strict_equality = true
explicit_package_bases = true
exclude = [
  '^\.git/',
  '^\.venv/',
  '^venv/',
  '^build/',
  '^dist/',
  '^\.mypy_cache/',
  '^\.pytest_cache/',
  '^\.ruff_cache/',
]
